[Service]
EnvironmentFile=/etc/reserve-sin/caddy.env
# Caddy expands variables from EnvironmentFile without --environ. Omitting the
# flag prevents its startup diagnostic from writing those values to journald.
ExecStart=
ExecStart=/usr/bin/caddy run --config /etc/caddy/Caddyfile
